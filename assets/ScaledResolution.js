export class ScaledResolution {

  /**
   * @param {int} width e.g. 1920
   * @param {int} height e.g. 1080
   */
  constructor(width, height) {
    this.width = width;
    this.height = height;
  }
}
